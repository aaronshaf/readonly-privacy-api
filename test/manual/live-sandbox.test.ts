import { describe, it } from "vitest";

describe("manual live sandbox test", () => {
  it.skip("calls Privacy sandbox only when explicitly enabled with local secrets", async () => {
    // This test is intentionally skipped by default.
    // Run manual live checks outside CI using local secret configuration.
  });
});
